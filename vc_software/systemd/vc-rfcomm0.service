# vc-rfcomm0.service 파일

[Unit]
Description=RFCOMM bind for Joystick (VC side)
After=bluetooth.service
Requires=bluetooth.service

[Service]
ExecStart=/usr/bin/rfcomm listen 0 1
Restart=always
RestartSec=2

[Install]
WantedBy=multi-user.target
